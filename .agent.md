---
name: picgrabber
description: 专门用于 PicGrabber（一键存图）Chrome 扩展开发。当您需要开发、调试或维护此扩展时使用此代理，例如：编写 manifest.json、content scripts、background scripts、实现图片抓取功能、处理浏览器 API 等。
argument-hint: 一个具体的开发任务，例如："添加图片过滤功能"、"修复下载失败问题"、"优化扩展性能"等。
---

<!-- Tip: 使用 /create-agent 在聊天中生成内容并获得代理协助 -->
## 项目基本信息

* 项目类型：Chrome 浏览器扩展（PicGrabber / 一键存图）
* 技术栈：
  - HTML/CSS/JavaScript
  - Chrome Extension API
  - Manifest V3
  - 其他：Webpack、Babel（可选）
* 主要功能：在 Chrome 浏览器中抓取页面图片并下载到指定目录
* 主要用户：需要从网页上下载图片的用户
* 主要目标：实现一个高效、易用的 Chrome 扩展，满足用户的图片下载需求
* 代码规范：遵循 JavaScript 最佳实践，模块化开发，禁止全局变量，禁止使用过时的 API
* 版本控制：Git，分支策略为 Git Flow
* 代码托管：GitHub
* 主要文件：
  - manifest.json：扩展的配置文件，定义权限、脚本、图标等
  - background.js：后台脚本，处理扩展的核心逻辑
  - content.js：内容脚本，注入到网页中抓取图片
  - popup.html/js：扩展的弹出界面，提供用户交互
  - options.html/js：扩展的选项页面，允许用户配置下载目录等
## 主要功能模块
1. 图片抓取模块：在 content.js 中实现，使用 DOM API 抓取页面上的图片元素，提取图片 URL
2. 下载模块：在 background.js 中实现，使用 Chrome 下载 API 将图片下载到指定目录
3. 用户界面模块：在 popup.html/js 中实现，提供一个按钮触发图片抓取和下载功能，在 options.html/js 中实现，允许用户设置下载目录
4. 权限管理：在 manifest.json 中声明必要的权限，例如 "downloads"、"activeTab" 等
5. 错误处理：在各个模块中实现错误处理机制，例如网络错误、权限不足等，并提供用户友好的提示
6. 性能优化：确保扩展在抓取和下载大量图片时仍能保持良好的性能，避免阻塞浏览器
## 项目结构
```chrome-plugin/
├── manifest.json
├── background.js
├── content.js
├── popup.html
├── popup.js
├── options.html
├── options.js
├── icons/
│   ├── icon16.png
│   ├── icon48.png
│   └── icon128.png
├── styles/
│   ├── popup.css
│   └── options.css
├── utils/
│   └── helper.js
├── tests/
│   ├── background.test.js
│   ├── content.test.js
│   ├── popup.test.js
│   └── options.test.js

```

## 其他说明
* 需要遵循 Chrome 扩展开发的最佳实践，确保扩展的安全性和稳定性
* 需要编写单元测试和集成测试，确保功能的正确性和可靠性
* 需要编写详细的文档，包括安装指南、使用说明、开发文档等，方便用户和开发者理解和使用扩展
* 需要定期更新和维护扩展，修复 bug、添加新功能、优化性能
* 需要关注用户反馈，及时响应用户的问题和建议，持续改进扩展的用户体验
* 需要遵守 Chrome Web Store 的政策和指南，确保扩展的合规性和可发布性

## 编码规范

**基础规范**
* 使用 ES6+ 语法
* 使用 `const` 和 `let`，禁止使用 `var`
* 使用箭头函数
* 使用模板字符串
* 使用解构赋值

**命名规范**
* 变量/函数：小驼峰 `camelCase`
* 常量：全大写加下划线 `UPPER_SNAKE_CASE`
* 类/构造函数：大驼峰 `PascalCase`
* 私有方法/变量：前缀下划线 `_privateMethod`

**代码风格**
* 使用分号
* 使用单引号字符串
* 代码缩进：2 空格
* 禁止一行代码超过 80 列

**示例**
```javascript
// ✅ 正确
const MAX_RETRY_COUNT = 3;
const imageUrls = [];
function downloadImages(urls) { ... }
class ImageDownloader { ... }

// ❌ 错误
var maxRetryCount = 3;
var imageUrls = [];
function downloadImages(urls) { ... }
```

## Manifest V3 配置示例

```json
{
  "manifest_version": 3,
  "name": "图片批量下载器",
  "version": "1.0.0",
  "description": "一键下载网页上的所有图片",
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  },
  "action": {
    "default_popup": "popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png"
    }
  },
  "permissions": [
    "downloads",
    "activeTab",
    "storage",
    "tabs"
  ],
  "host_permissions": [
    "<all_urls>"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ],
  "options_page": "options.html"
}
```

## 常用命令

```bash
# ========== 开发调试 ==========
# 加载未打包扩展
# 1. 打开 Chrome -> chrome://extensions/
# 2. 开启"开发者模式"
# 3. 点击"加载已解压的扩展程序"
# 4. 选择 chrome-plugin 目录

# 重新加载扩展
# 在扩展管理页面点击刷新按钮

# 查看后台脚本日志
# chrome://extensions/ -> "service worker" 链接

# ========== 测试 ==========
# 安装测试依赖
npm install

# 运行单元测试
npm test

# 运行测试并生成覆盖率报告
npm run test:coverage

# ========== 构建 ==========
# 如果使用 Webpack
npm run build

# 打包发布
# 1. 压缩 chrome-plugin 目录为 zip
# 2. 上传至 Chrome Web Store
```

## 调试技巧

**1. 调试 Content Script**
* 打开目标网页
* 右键 -> 检查
* 切换到 Sources 面板
* 找到 content.js 文件设置断点

**2. 调试 Background Script**
* 打开 `chrome://extensions/`
* 找到扩展，点击 "service worker" 链接
* 在 DevTools 中调试

**3. 调试 Popup**
* 右键扩展图标 -> 检查弹出窗口
* 在 DevTools 中调试

**4. 查看下载文件**
* 打开 Chrome 下载页面 `chrome://downloads/`
* 或使用 `chrome.downloads.show()` API

**5. 常见问题排查**
* 权限不足：检查 manifest.json 中的 permissions
* 跨域问题：使用 chrome.runtime.sendMessage 通信
* 内存泄漏：及时移除事件监听器

## 性能优化建议

**1. 图片抓取优化**
```javascript
// 使用 MutationObserver 监听动态加载的图片
const observer = new MutationObserver((mutations) => {
  mutations.forEach((mutation) => {
    mutation.addedNodes.forEach((node) => {
      if (node.tagName === 'IMG') {
        // 处理新图片
      }
    });
  });
});

observer.observe(document.body, {
  childList: true,
  subtree: true,
  attributes: true,
  attributeFilter: ['src']
});
```

**2. 下载队列控制**
```javascript
class DownloadQueue {
  constructor(maxConcurrent = 3) {
    this.queue = [];
    this.active = 0;
    this.maxConcurrent = maxConcurrent;
  }

  add(url) {
    this.queue.push(url);
    this.process();
  }

  async process() {
    if (this.active >= this.maxConcurrent) return;
    
    const url = this.queue.shift();
    if (!url) return;
    
    this.active++;
    await this.download(url);
    this.active--;
    this.process();
  }
}
```

**3. 避免内存泄漏**
```javascript
// ✅ 正确：及时清理
class ImageScanner {
  constructor() {
    this.observer = null;
  }

  start() {
    this.observer = new MutationObserver(this.handleMutations);
    this.observer.observe(document.body, { childList: true });
  }

  stop() {
    if (this.observer) {
      this.observer.disconnect();
      this.observer = null;
    }
  }
}

// ❌ 错误：未清理
class BadScanner {
  start() {
    this.observer = new MutationObserver(this.handleMutations);
    this.observer.observe(document.body, { childList: true });
  }
  // 缺少 stop 方法
}
```

## 测试规范

**单元测试示例**
```javascript
// tests/content.test.js
describe('ImageScanner', () => {
  let scanner;
  
  beforeEach(() => {
    scanner = new ImageScanner();
  });
  
  afterEach(() => {
    scanner.stop();
  });
  
  it('should find all images on page', () => {
    document.body.innerHTML = `
      <img src="image1.jpg">
      <img src="image2.jpg">
    `;
    
    const images = scanner.scan();
    
    expect(images).to.have.lengthOf(2);
  });
  
  it('should filter images by extension', () => {
    document.body.innerHTML = `
      <img src="image1.jpg">
      <img src="image2.png">
      <img src="image3.gif">
    `;
    
    const images = scanner.scan({ extensions: ['jpg', 'png'] });
    
    expect(images).to.have.lengthOf(2);
  });
});
```

## Agent 行为约束

* 遵循 Manifest V3 规范
* 使用 Chrome Extension API 而非过时的 API
* 禁止在 content scripts 中使用 `eval()`
* 敏感信息存储在 `chrome.storage` 而非 localStorage
* 使用 `chrome.runtime.sendMessage` 进行通信
* 错误信息对用户友好，不暴露敏感技术细节
* 生成的代码可直接运行，无语法错误


